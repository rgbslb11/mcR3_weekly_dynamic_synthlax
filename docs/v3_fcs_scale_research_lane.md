# V3 FCS point-scale research lane (R1)

Lane: `claude/v3-fcs-scale-research-r1`
Blocker studied: `model_scale.FCS_ELO_1250_TO_V3_POINT_SCALE_ADAPTER`
Terminal: **`FCS_SCALE_RESEARCH_READY_FOR_INPUTS`**
Harness: **`FROZEN_READY_FOR_INPUTS`**
Promotion authorised: **false**. Blockers before 8, after 8.
Chairman ruling required for FCS scale estimation: **no**.

## The question

> What V3 neutral-field football-point representation of the fixed FCS Elo 1250
> best explains real FBS-versus-FCS results?

FCS Elo is **1250**, permanent and current. This lane does not reopen it, does
not replace it, and does not reinterpret 0.294, 0.297 or 0.297514 as the FCS
translation — all three are refused by name, and the inversion of the *governed*
1250 through the Elo/Board transform is refused by reconstruction so the refusal
does not rest on a blacklist of three literals.

What is missing is the model-scale adapter, and the intended route to it was
already named in `fcs.RECOGNISED_SCALE_AUTHORITIES`: `MARGIN_CALIBRATED_HOLDOUT`.
This lane builds the harness that route needs. It does not run it, because two
of its inputs do not exist.

## What was built

`src/ncaaf_engine/simulation/dynamic_weekly_mc_v3/fcs_scale_research.py`

| Piece | What it does |
| --- | --- |
| `bind_research_corpus` | Binds a corpus only against re-read bytes, a matching SHA-256, an admissible source-authority class and a human audit token. |
| `FbsVsFcsObservation` | The observation contract. Holds a real game that is *structurally ready and numerically unavailable* rather than forcing a corpus to choose between fabricating a field and dropping the game. |
| `expected_fbs_margin` | The governed transform, re-oriented onto the FBS side. |
| `search_fcs_point_baseline` | Deterministic coarse-to-fine grid with boundary expansion, boundary reporting and flat-surface detection. |
| `baseline_interval` / `precision_envelope` | Exact Student-t interval, stdlib only. The envelope is a design property that consumes a dispersion and a count. |
| `leave_season_out` / `temporal_holdout` | Whole-season validation. No random row split. |
| `season_sensitivity` / `venue_sensitivity` | Refits under each season removal and each admissible venue subset. |
| `unit_slope_witness` | Diagnostic for axis mis-scaling. Never an adapter parameter. |
| `identification_verdict` | `IDENTIFIED` / `WEAKLY_IDENTIFIED_RANGE_ONLY` / `NOT_IDENTIFIED`, with each failing condition named. |

Artifact: `reference/dynamic_weekly_mc_v3/V3_FCS_SCALE_RESEARCH_R1.json`,
regenerated byte-for-byte by `scripts/build_fcs_scale_research_r1.py`. A test
asserts the committed copy equals a fresh emission.

## The governed orientation

Transcribed from `game.simulate_game`, not paraphrased:

```
hfa = 0.0 if game.venue == "NEUTRAL" else hfa_baseline_points * home_hfa_modifier
expected = home.current_strength_points - away.current_strength_points + hfa
```

Production is home-oriented. Re-oriented onto the FBS side:

```
expected_fbs_margin = fbs_pregame_points - fcs_point_baseline + venue_term
venue_term          = sign * hfa_baseline_points * home_hfa_modifier
sign                = +1 at FBS HOME, -1 at FCS HOME, and the term is exactly 0.0 at NEUTRAL
```

`NEUTRAL` is exactly `0.0`, matching production, and consumes no modifier. There
is no `float(... or 1.0)` anywhere: a missing venue, a missing HFA baseline and a
missing home-field modifier each refuse. When the FCS side is at home the
modifier being asked for belongs to an FCS entity, and POWER_CRUNCH records every
one of those as `UNRESOLVED`, so that case routes through
`fcs.require_fcs_hfa_modifier` and refuses with that reason.

## Why no number was produced

### 1. The historical FBS point states do not exist yet

`HISTORICAL_STRENGTH_AXIS_ANCHOR` is classified **`EMPIRICALLY_CALIBRATABLE`**:
the historical point-axis scale is recoverable from real Week 1-2 margins once
historical opening standardized states are available. Opening weeks are the
right place for it because they run on preseason opening strength, so their
expected margins consume no weekly rerating coefficient and the axis is not
confounded with parameters that are themselves unfitted.

That classification **supersedes** an earlier `HUMAN_GOVERNANCE_REQUIRED`
reading, which an independent audit of the historical expected-margin lane
refuted. **No Chairman ruling is a prerequisite for FCS scale estimation.** This
lane waits on a calibration result, not on an authority.

What did not change is the mathematics, and it is the reason this lane still
refuses to fit against an unestablished axis. The FCS baseline enters the
expected margin **additively**, so its least-squares estimate is

```
F_hat = mean(fbs_pregame_points + venue_term - actual_margin_fbs)
```

Add a constant `c` to every historical FBS point state and `F_hat` moves by
exactly `c` while every residual, every RMSE and the whole objective surface stay
identical. The fitted value therefore estimates *the FCS baseline plus the axis
offset*, and no sample size separates them. A test checks this rather than
asserting it.

So the axis has to be established — by calibration, which is the available route
— *before* a fitted baseline means the FCS baseline. That is an ordering
constraint on the estimation, not a governance gate.

The axis **scale** is different: it leaves a signature. Scaling the axis by `k`
makes the regression of venue-adjusted margin on FBS strength read slope `1/k`
instead of `1`. `unit_slope_witness` reports it as a diagnostic. It is never
fitted into the adapter — the governed FCS model is one fixed Elo mapped to one
point value, and adding a scale parameter would be a different model this lane
has no authority to propose.

### 2. The corpus is a candidate, not evidence

`claude/v3-historical-observation-corpus-r6` at
`fb817a6a08df598038966c8f5fe345153dd302f3` identifies **43 real FBS-versus-FCS
observations** across 2021–2024 (11 / 9 / 12 / 11), involving 10 distinct FCS
participants. Its own terminal is `HISTORICAL_OBSERVATION_CORPUS_R6_READY_FOR_AUDIT`
and it is not merged. That is a request for an audit, not the result of one, so
`bind_research_corpus` refuses it. Nothing was copied from that worktree; the
committed artifacts were read at that exact SHA to *assess* admissibility, and
the assessment is recorded in `CANDIDATE_CORPUS_ASSESSMENT`.

### 3. Venue is unavailable for all 43

The NCAA scoreboard feed publishes no neutral-site indicator. The candidate's
own remaining-blocked list says so: margins are signed from the designated home
team's perspective and neutral sites are indistinguishable from home games.

So the venue ledger for the candidate reads:

| bucket | count |
| --- | --- |
| `usable_home` | 0 |
| `usable_away` | 0 |
| `usable_neutral` | 0 |
| `venue_ambiguous_excluded` | 43 |

A neutral site scored as a home game inflates the venue term by the full
governed 3.5-point HFA, and that displacement lands **entirely** in `F_hat`:
`displacement = misclassified_share × 3.5`. Ten percent misclassification moves
the baseline 0.35 points; a wholly wrong classification moves it 3.5.

There is a second venue finding that matters for the design. All 43 place the
FCS entity on the designated-**away** side — `fcs_designated_home` is **0**. So
even a perfect venue classification of this candidate would give the FCS-home
orientation zero support, while the 2026 schedule contains three real FCS-home
games (G0019, G0213, G0224) that production must handle. The harness tests all
three orientations; the candidate evidence can only ever exercise one.

## What the 43 games could deliver, once the axis is established

A design calculation, not a fit. It consumes only a dispersion and a count:

* Unconditional margin SD over the 43: **24.4831** points (mean 23.6512, 7 FCS wins).
* `t(0.975, 42)` = 2.0181, so the 95% half-width is at most **±7.53 points**.

That is an *upper* bound — conditioning on FBS strength can only reduce
dispersion, and a realistic residual SD near 20 gives roughly ±6.2. Against an
observed FBS point span of 50.34 (−17.4576 to +32.8847), a ±6-to-7.5 point
interval is 12–15% of the whole FBS range and about twice the governed HFA.

Under this lane's material threshold of 4.0 points of half-width, 43 games at
this dispersion return `WEAKLY_IDENTIFIED_RANGE_ONLY` — a defensible **range**,
not a promotable point value. At the same dispersion,
`observations_required_for_half_width` computes **147** observations for a
±4-point half-width and **579** for ±2. Both are upper bounds for the same
reason the ±7.53 is: they use the unconditional SD.

## Freeze

The harness phase is complete and the harness is frozen as an **input-ready
research tool** — `HARNESS_STATUS = FROZEN_READY_FOR_INPUTS`. Frozen means its
mathematics, its refusals and its search are settled and are not to be
redesigned. It does not mean sealed: handing it the three inputs below runs the
study through exactly this code, which is why it is frozen in a runnable state
rather than a half-built one.

`PRESERVED_FINDINGS` pins the six results this lane established, so a later
change that contradicts one of them fails a test instead of passing quietly:

1. `F_hat = mean(FBS_pregame_points + venue_adjustment − actual_margin_FBS)`.
2. FCS Elo = 1250, fixed.
3. The 43 candidate observations give weak, range-level identification — not
   high-precision standalone identification.
4. All 43 are currently FBS-designated-home.
5. A constant shift in the FBS point-axis zero shifts `F_hat` one-for-one while
   preserving every game residual.
6. Venue misclassification directly biases the fitted FCS baseline.

## Numerical input dependencies

`FCS_SCALE_RESEARCH_READY_FOR_INPUTS`. Three inputs, all of them **evidence**
dependencies. **None is an authority gate, and no Chairman ruling is a
prerequisite for FCS scale estimation.**

1. Governed / empirically established **historical FBS pregame V3 point
   states**, declaring `strength_domain = V3_UNIFIED_NEUTRAL_FIELD_POINTS`.
2. **Audited real FBS-vs-FCS observations**, bound by exact SHA through
   `bind_research_corpus`.
3. **Defensible venue classification** for every observation used — or explicit
   exclusion of the ambiguous ones.

Then call `fit_fcs_point_baseline`, and
`fcs_scale_research_result(corpus=…, observations=…)` for the artifact.

## Scope

Nothing here retires a blocker, writes canonical configuration, registers an
adapter, introduces team-specific or season-specific FCS ratings, fits any
calibration parameter, or runs a season simulation. Blockers 8 before, 8 after.

Even a clean run recommends at most a candidate. Installing it still requires
`fcs.register_fcs_scale_adapter` with a `MARGIN_CALIBRATED_HOLDOUT` provenance
and a human `APPROVE_V3_FCS_SCALE_ADAPTER::<RULING_ID>` token, neither of which
this lane can produce. That approval gate governs *promotion*, which is a
different question from whether the scale can be estimated at all.
